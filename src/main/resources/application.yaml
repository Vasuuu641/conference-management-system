spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/conference_db
    username: conference_user
    password: admin123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  thymeleaf:
    cache: false

server:
  port: 8080